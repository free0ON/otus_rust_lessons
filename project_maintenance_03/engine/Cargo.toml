[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
# rand = { version = "0.9.2" }
rand = { version = "0.9.2", optional = true}

[features]
# real_rand = []
real_rand = ["rand"]
default = ["real_rand"]
